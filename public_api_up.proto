syntax = "proto3";

import "public_api_types.proto"; 

message APIUp {
    oneof status{
        BaseStatus base_status = 1;
        // todo add arm, lifter, etc, if needed
    }
    repeated MotorStatus motor_status = 2;
}
